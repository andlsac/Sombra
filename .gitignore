# --- Artefatos de build (recriados por scripts/bundle.sh) ---
/.build/
/build/
/dist/

# --- Dependências vendadas/baixadas (recriadas por scripts/build_llama.sh) ---
/vendor/
/Frameworks/

# --- Modelos baixados (recriados por scripts/download_model.sh) ---
/models/

# --- Binários grandes em qualquer lugar (limite do GitHub: 100 MB) ---
*.gguf
*.dylib
*.dmg
*.metallib

# --- macOS ---
.DS_Store

# Pasta de trabalho de imagens (a versão limpa fica em assets/)
prints e imagens/

# Estado pessoal do Xcode (UI, schemes) — muda a cada abertura
xcuserdata/

imagens/
